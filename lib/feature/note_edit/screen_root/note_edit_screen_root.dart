import 'package:flutter/material.dart';

class NoteEditScreenRoot extends StatelessWidget {
  const NoteEditScreenRoot({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
